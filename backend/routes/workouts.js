
const express = require('express');
const router = express.Router();

// Placeholder for workouts routes
router.get('/', (req, res) => {
    res.json({message: "Workouts route placeholder"});
});

module.exports = router;
